version: 1

tasks:
  backend: "minsky"

repository:
  backend: "github"
  url: "https://github.com/edobry/minsky.git"
  github:
    owner: "edobry"
    repo: "minsky"

workflows:
  lint:
    # Use local eslint to avoid incompatibility with the globally-installed eslint (v10 vs v9)
    jsonCommand: "./node_modules/.bin/eslint . --format json"
