version: 1

# Task Backend Configuration - Use tasks.backend instead of deprecated root backend property
# backend: removed - deprecated property, use tasks.backend instead
tasks:
  backend: "minsky"  # Modern task backend configuration
sessiondb:
  postgres:
    connectionString: "postgresql://localhost:5432/minsky"
