---
# Generated by minsky rules compile. Do not edit directly.
name: Key Architecture
description: 'Minsky architectural overview - clean architecture layers, command routing, DI, persistence providers.'
alwaysApply: true
tags:
  - minsky
  - architecture
---
# Key Architecture

- Clean architecture: Domain → Adapters → Infrastructure
- Shared command registry: commands defined once, adapted to CLI and MCP
- Capability-based persistence providers (ADR-002)
- Multi-backend tasks: GitHub Issues, Minsky DB
- Dependency injection via tsyringe (`docs/architecture.md` §6)

## Operational reference rules (on demand, not always-loaded)

- `mcp-disconnect-cadence` — disconnect cause classes, escalation thresholds, log-reading recipes (read when investigating MCP disconnects)
- `subagent-dispatch-cadence` — dispatch outcome taxonomy, escalation thresholds, SQL inspection patterns (read when investigating subagent outcomes)
- `documentation-taxonomy` — doc-type taxonomy, homes, title patterns (read when authoring docs; `/create-task`, `/draft-rfc`, `/draft-adr` carry the workflow)

Read via `.minsky/rules/<name>.mdc` or `mcp__minsky__rules_get`.