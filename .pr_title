feat(#303): implement auto-commit integration for task operations

Implements complete auto-commit functionality for task operations in markdown backend, eliminating need for manual git commits after task status updates, creation, or deletion.