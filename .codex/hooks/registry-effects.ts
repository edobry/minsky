#!/usr/bin/env bun
// Generated by minsky compile. Do not edit directly.
// Source: .minsky/hooks/registry-effects.ts

// SC5 effect-declaration factories + posture constants — extracted from
// `registry.ts` (mt#3658).
//
// WHY THEY MOVED. `registry.ts` reached its 1500-line `max-lines` ceiling: the
// file sat at 1499 counted lines, so the NEXT guard registration — any guard,
// not this one in particular — could not land. Per mem#833, a shared file at
// its ceiling is extracted rather than shaved, because shaved margin is
// consumed by the next concurrent merge. Comments do not count toward the
// ceiling (`skipComments`), so trimming prose buys nothing; only moving code
// does.
//
// These factories are the right thing to move rather than a slice of the
// registrations, for two reasons. They are already a self-contained concern
// with a second consumer outside this file (mt#3981 exported them so
// `scripts/lib/standalone-guard-canaries.ts` builds its `effects` from the
// SAME defaults rather than a hand-copied mirror). And moving them creates no
// import cycle, whereas moving registrations would: an extracted registration
// module needs these factories, and they live in the file that would import it
// back.
//
// `registry.ts` re-exports everything here, so every existing import site is
// unchanged and this extraction is invisible to callers.
//
// @see docs/architecture/adr-028-guard-hook-dispatcher-consolidation.md — D2
// @see mt#3981 — the per-effect failurePolicy/verdictShape triad these encode

// TYPE-only import from `./registry`, which imports this module back for the
// factories' VALUES. That is not a cycle at runtime: type imports are erased,
// so the emitted edge runs one way (registry → registry-effects). Keeping the
// types where they are also keeps `GuardRegistration`'s neighbours together —
// moving them would scatter one interface family across two files to no end.
import type { EffectFailurePosture, GuardEffectDeclaration } from "./registry";

/** SC5 default: enforcement effects (deny/allow decisions) fail closed on both axes. */
export const ENFORCEMENT_POSTURE: EffectFailurePosture = {
  failurePolicy: "closed",
  degradedPolicy: "closed",
};

/** SC5 default: advisory injector effects fail open on both axes — a thinner turn, never a blocked one. */
export const ADVISORY_POSTURE: EffectFailurePosture = {
  failurePolicy: "open",
  degradedPolicy: "open",
};

/** SC5 default: recorder effects spool locally on both axes — today's JSONL calibration pattern. */
export const RECORDER_POSTURE: EffectFailurePosture = {
  failurePolicy: "spool",
  degradedPolicy: "spool",
};

/** A `"deny"` validator effect at the SC5 enforcement default (closed/closed). */
export function enforcementEffect(
  effect: GuardEffectDeclaration["effect"] = "deny"
): GuardEffectDeclaration {
  return { effect, verdictShape: "validator", failurePolicy: ENFORCEMENT_POSTURE };
}

/** An `"additionalContext"`/`"sessionTitle"` injector effect at the SC5 advisory default (open/open). */
export function advisoryEffect(
  effect: GuardEffectDeclaration["effect"] = "additionalContext",
  rationale?: string
): GuardEffectDeclaration {
  return {
    effect,
    verdictShape: "injector",
    failurePolicy: ADVISORY_POSTURE,
    ...(rationale ? { rationale } : {}),
  };
}

/** A `"calibration"`/out-of-band-write recorder effect at the SC5 default (spool/spool). */
export function recorderEffect(
  effect: GuardEffectDeclaration["effect"] = "calibration",
  rationale?: string
): GuardEffectDeclaration {
  return {
    effect,
    verdictShape: "recorder",
    failurePolicy: RECORDER_POSTURE,
    ...(rationale ? { rationale } : {}),
  };
}

/** A payload-transform (`updatedInput`) mutator effect — no SC5 default; posture is always explicit. */
export function mutatorEffect(
  effect: GuardEffectDeclaration["effect"],
  posture: EffectFailurePosture,
  rationale?: string
): GuardEffectDeclaration {
  return {
    effect,
    verdictShape: "mutator",
    failurePolicy: posture,
    ...(rationale ? { rationale } : {}),
  };
}
