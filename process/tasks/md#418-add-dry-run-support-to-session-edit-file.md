# Add dry-run support to session.edit_file

## Context

Add dryRun=true option to session.edit_file MCP tool that returns proposed edited content and a concise unified diff without writing to disk; include tests and CLI examples; ensure no markers in final output and preserve formatting.

## Requirements

## Solution

## Notes
