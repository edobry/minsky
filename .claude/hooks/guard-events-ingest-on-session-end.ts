#!/usr/bin/env bun
// Generated by minsky compile. Do not edit directly.
// Source: .minsky/hooks/guard-events-ingest-on-session-end.ts

// SessionEnd hook: run one guard-events ingest sweep tick (mt#4035, mt#3334
// phase 3) so the guard/calibration exhaust becomes queryable promptly.
//
// THE SWEEP IS THE CORRECTNESS LAYER; THIS HOOK IS A LATENCY OPTIMIZATION
// ONLY. Per ADR-017 and mt#2313, SessionEnd does not fire (or complete) on
// `/exit`, `/clear`, or an async kill — so this hook covers only the subset
// of turns that end cleanly. Completeness comes from the periodic cockpit
// sweeper (`startGuardEventsSweepBackstop`, `src/cockpit/sweepers.ts`), which
// calls the exact same `guard-events.ingest` command on a fixed cadence
// regardless of whether any SessionEnd event ever fires. Mirrors
// `transcript-ingest-on-session-end.ts`'s two-layer shape for a different
// corpus.
//
// Every stream's dedupe key (sha256 over `<stream>\n<verbatim line>`) makes
// re-running the ingest over an already-ingested span a no-op, which is what
// makes firing this from BOTH a fragile per-session hook and a resilient
// periodic sweep safe — constraint #5 ("a full re-scan must remain SAFE via
// dedupe").
//
// Observability: every run appends a JSON line to
// <state-dir>/guard-events-ingest-hook-log.jsonl recording the ingest exit
// code and (on failure) stderr — same convention as the transcript hook's
// own log, so a failed ingest leaves a signal rather than being silently
// swallowed (SC2).
//
// Override: MINSKY_SKIP_GUARD_EVENTS_INGEST_HOOK=1|true|yes skips the hook
// with an audit line to stdout. The hook ALWAYS exits 0 — SessionEnd is a
// no-decision-control event and must never block session teardown.
//
// @see mt#4035 — this hook
// @see mt#4034 — the guard_events table this feeds
// @see docs/architecture/guard-calibration-stream-inventory.md — the stream set
// @see .minsky/hooks/transcript-ingest-on-session-end.ts — the precedent this mirrors

import { homedir } from "node:os";
import { join, dirname } from "node:path";
import { appendFileSync, mkdirSync } from "node:fs";
import { readInput } from "./types";
import type { ClaudeHookInput } from "./types";

export const GUARD_EVENTS_INGEST_OVERRIDE_ENV = "MINSKY_SKIP_GUARD_EVENTS_INGEST_HOOK";
export const HOOK_LOG_FILENAME = "guard-events-ingest-hook-log.jsonl";
export const INGEST_TIMEOUT_MS = 20_000;

export interface CommandResult {
  exitCode: number;
  stdout: string;
  stderr: string;
  timedOut?: boolean;
}

export interface IngestDeps {
  runCommand: (cmd: string[], opts: { timeoutMs: number }) => CommandResult;
  appendLog: (logPath: string, line: string) => void;
  resolveLogPath: () => string;
  now: () => Date;
  minskyBin: string;
}

export function isTruthy(value: string | undefined): boolean {
  if (!value) return false;
  const v = value.toLowerCase();
  return v === "1" || v === "true" || v === "yes";
}

function truncate(s: string, max: number): string {
  return s.length > max ? `${s.slice(0, max)}…[truncated]` : s;
}

export interface IngestOutcome {
  ingestExitCode: number;
  ingestTimedOut: boolean;
}

/**
 * Core hook logic. Pure with respect to injected deps so tests drive it with
 * fakes. Never throws — every failure path is recorded to the log and folded
 * into the returned outcome. The entrypoint always exits 0.
 */
export function runGuardEventsIngestOnSessionEnd(
  _input: ClaudeHookInput,
  deps: IngestDeps
): IngestOutcome {
  const logPath = deps.resolveLogPath();
  const ts = deps.now().toISOString();

  const writeRecord = (record: Record<string, unknown>): void => {
    try {
      deps.appendLog(
        logPath,
        `${JSON.stringify({ timestamp: ts, event: "session_end", ...record })}\n`
      );
    } catch {
      // Logging is best-effort; never let it break the hook.
    }
  };

  let ingest: CommandResult;
  try {
    ingest = deps.runCommand([deps.minskyBin, "guard-events", "ingest"], {
      timeoutMs: INGEST_TIMEOUT_MS,
    });
  } catch (err) {
    writeRecord({ ingest: { error: err instanceof Error ? err.message : String(err) } });
    return { ingestExitCode: 1, ingestTimedOut: false };
  }

  const ingestOk = ingest.exitCode === 0 && !ingest.timedOut;
  writeRecord({
    ingest: {
      exitCode: ingest.exitCode,
      timedOut: ingest.timedOut ?? false,
      ...(ingestOk ? {} : { stderr: truncate(ingest.stderr || ingest.stdout, 2000) }),
    },
  });

  return { ingestExitCode: ingest.exitCode, ingestTimedOut: ingest.timedOut ?? false };
}

// ── Real dependency wiring ──────────────────────────────────────────────────

function resolveStateDir(env: Record<string, string | undefined>): string {
  const stateDir = env.MINSKY_STATE_DIR;
  return stateDir && stateDir.trim().length > 0
    ? stateDir
    : join(homedir(), ".local", "state", "minsky");
}

function realRunCommand(cmd: string[], opts: { timeoutMs: number }): CommandResult {
  const pathPrefix = `${join(homedir(), ".bun", "bin")}:/opt/homebrew/bin:/usr/local/bin:${process.env.PATH ?? ""}`;
  const result = Bun.spawnSync(cmd, {
    stdout: "pipe",
    stderr: "pipe",
    timeout: opts.timeoutMs,
    env: {
      MINSKY_PERSISTENCE_POSTGRES_CONNECT_TIMEOUT: "2",
      ...process.env,
      PATH: pathPrefix,
    },
  });
  // Explicit, documented signal check (mt#4035 R1) — mirrors the CANONICAL,
  // already-fixed detection in `.minsky/hooks/types.ts`'s `safeSpawnSync`
  // (mt#2810 PR #1952 R1 NON-BLOCKING): a timed-out `Bun.spawnSync` always
  // reports `exitCode: null` (it never completed normally), but the signal
  // Bun uses to kill it can vary by platform/version (e.g. SIGKILL after a
  // grace period). Gating on `signalCode === "SIGTERM"` specifically — the
  // narrower check this hook used before this fix, and the one
  // transcript-ingest-on-session-end.ts's own inline copy still uses —
  // produces a FALSE NEGATIVE (`timedOut: false`) for any of those other
  // signals. `exitCode === null` alone is the reliable "did not exit
  // normally" signal; that is the only condition Bun's docs guarantee for a
  // timeout-killed process.
  const timedOut = result.exitCode === null;
  return {
    exitCode: result.exitCode ?? 1,
    stdout: result.stdout.toString().trim(),
    stderr: result.stderr.toString().trim(),
    timedOut,
  };
}

async function main(): Promise<void> {
  const env = process.env;

  if (isTruthy(env[GUARD_EVENTS_INGEST_OVERRIDE_ENV])) {
    process.stdout.write(
      `[guard-events-ingest-on-session-end] override active: ${GUARD_EVENTS_INGEST_OVERRIDE_ENV}=${env[GUARD_EVENTS_INGEST_OVERRIDE_ENV]} at ${new Date().toISOString()}\n`
    );
    return;
  }

  let input: ClaudeHookInput;
  try {
    input = await readInput<ClaudeHookInput>();
  } catch {
    return; // garbage-in → no-op
  }

  runGuardEventsIngestOnSessionEnd(input, {
    runCommand: realRunCommand,
    appendLog: (logPath, line) => {
      mkdirSync(dirname(logPath), { recursive: true });
      appendFileSync(logPath, line);
    },
    resolveLogPath: () => join(resolveStateDir(env), HOOK_LOG_FILENAME),
    now: () => new Date(),
    minskyBin: "minsky",
  });
}

if (import.meta.main) {
  await main();
}
