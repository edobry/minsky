#!/usr/bin/env bun
// Generated by minsky compile. Do not edit directly.
// Source: .minsky/hooks/interceptor-provenance-paths.ts

// The repo-relative paths interceptor descriptions cite as provenance — mt#4198.
//
// Split out of `interceptor-descriptions.ts` when the description store outgrew
// the 1500-line `max-lines` ERROR ceiling and its settings-registered cohort
// moved into `interceptor-descriptions-settings.ts`. Both modules cite the same
// handful of paths, and a second hand-maintained copy is exactly the drift
// `DELIBERATELY_UNAUTHORED_NAMES` was consolidated to avoid (PR #2914 R1): the
// first symptom is one surface pointing at a renamed rule while the other still
// resolves.
//
// A dependency-free leaf with no imports at all, so it can sit UNDER both
// description modules without any import cycle — the settings module needs these
// as VALUES, and importing them back from `interceptor-descriptions.ts` would
// evaluate them inside that module's own initialization.
//
// @see .minsky/hooks/interceptor-descriptions.ts — the store these annotate
// @see .minsky/hooks/interceptor-descriptions-settings.ts — the sibling cohort

export const REGISTRY = ".minsky/hooks/registry.ts";
export const HOOK_FILES_RULE = ".minsky/rules/hook-files.mdc";
export const HOOK_OBSERVERS_RULE = ".minsky/rules/hook-observers.mdc";
export const PRECOMMIT = "src/hooks/pre-commit.ts";
export const KNOWN_NAMES = ".minsky/hooks/known-guard-names.ts";

/** `.minsky/hooks/<name>.ts` — the SOURCE tree; `.claude/hooks/*` is generated. */
export const hook = (file: string): string => `.minsky/hooks/${file}.ts`;
