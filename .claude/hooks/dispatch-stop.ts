#!/usr/bin/env bun
// Generated by minsky compile. Do not edit directly.
// Source: .minsky/hooks/dispatch-stop.ts

// Stop dispatcher entrypoint — ADR-028 D1 (mt#2357).
//
// The SOLE `.claude/settings.json` Stop entry for guards registered on the
// dispatcher framework's `Stop` lifecycle event in `./registry.ts` —
// currently one: `turn-end-retro-scan` (the framework's first Stop-event
// guard). `typecheck-on-stop.ts` remains a deliberately-standalone sibling
// settings entry (it predates the framework and emits its own block
// decisions on its own budget model).
//
// Reads stdin once, resolves shared context once (D6 — transcript-candidate
// resolution included), and runs every matched guard's pure function in
// process via `runDispatcher` — see `./dispatcher.ts` for the core loop.
// The dispatcher's generic output shaping already emits
// `hookSpecificOutput.hookEventName = "Stop"`, which is the shape hooks.md
// requires for Stop-hook feedback (an advisory continuation beat).
//
// @see docs/architecture/adr-028-guard-hook-dispatcher-consolidation.md — D1
// @see .minsky/hooks/dispatcher.ts — the core dispatcher loop
// @see .minsky/hooks/registry.ts — the declarative guard registry
// @see .minsky/hooks/dispatch-userpromptsubmit.ts — the UserPromptSubmit sibling entrypoint
// @see mt#2357 — the task that introduced the Stop event's first guard

import { runDispatcher } from "./dispatcher";

if (import.meta.main) {
  try {
    await runDispatcher("Stop", { hookFilename: "dispatch-stop.ts" });
    process.exit(0);
  } catch (err) {
    process.stderr.write(
      `[dispatch-stop] fail-open: ${err instanceof Error ? err.message : String(err)}\n`
    );
    process.exit(0);
  }
}
