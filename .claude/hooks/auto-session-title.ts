#!/usr/bin/env bun
// Generated by minsky compile. Do not edit directly.
// Source: .minsky/hooks/auto-session-title.ts

// UserPromptSubmit hook: consume a session-label file dropped by another
// tool (e.g. session_start) and set the session's display title.
//
// @see .minsky/hooks/registry.ts — ADR-028 Phase 2b (mt#2687): `run()` below
//      is the dispatcher-compatible entry point invoked in-process by
//      `./dispatch-userpromptsubmit.ts`; the standalone `main()` /
//      `if (import.meta.main)` CLI entrypoint below matches every sibling
//      guard's gate (mt#2835 — this file was the ONE registered guard whose
//      module-level `main()` call was ungated: `main().catch(() =>
//      process.exit(0))` ran unconditionally at import time, so the
//      dispatcher's own dynamic `import("./auto-session-title")` re-entered
//      an already-drained stdin via `main()`'s own `readInput()`, which
//      eventually rejected and called `process.exit(0)` mid-dispatch-loop —
//      killing the whole dispatcher process, silently, before any guard
//      downstream of this one (all 14 others) ran or wrote output). This
//      guard's output is the one exception to the family's
//      `additionalContext` shape — a scalar `sessionTitle` (see
//      `GuardOutcome.sessionTitle` in `./registry.ts` and
//      `HookOutput.hookSpecificOutput.sessionTitle` in `./types.ts`).
import { readInput, writeOutput } from "./types";
import type { ClaudeHookInput, HookOutput } from "./types";
import { existsSync, readFileSync, unlinkSync } from "fs";
import type { DispatchContext, GuardOutcome } from "./registry";

/**
 * Dispatcher-compatible pure function (ADR-028 Phase 2b, mt#2687). Mirrors
 * `main()`'s orchestration but returns a `GuardOutcome` instead of writing to
 * stdout / calling `process.exit` — the dispatcher owns stdout and aggregates
 * every matched guard's output (D1). `ctx` is unused (this guard needs
 * neither the transcript nor host-cap budgets), matching the base
 * `ClaudeHookInput` guard-module contract.
 */
export function run(input: ClaudeHookInput, _ctx: DispatchContext): GuardOutcome | null {
  const sessionId = input.session_id;
  const labelFile = `/tmp/claude-session-label-${sessionId}.json`;

  if (!existsSync(labelFile)) return null;

  let taskId: string;
  let title: string;
  try {
    const contents = readFileSync(labelFile, "utf8");
    const data = JSON.parse(contents) as { taskId: string; title: string };
    taskId = data.taskId;
    title = data.title;
  } catch {
    try {
      unlinkSync(labelFile);
    } catch {
      // Ignore cleanup errors
    }
    return null;
  }

  try {
    unlinkSync(labelFile);
  } catch {
    // Ignore cleanup errors
  }

  return { sessionTitle: `${taskId} — ${title}` };
}

async function main(): Promise<void> {
  const input = await readInput<ClaudeHookInput>();
  const sessionId = input.session_id;

  const labelFile = `/tmp/claude-session-label-${sessionId}.json`;

  if (!existsSync(labelFile)) {
    process.exit(0);
  }

  let taskId: string;
  let title: string;

  try {
    const contents = readFileSync(labelFile, "utf8");
    const data = JSON.parse(contents) as { taskId: string; title: string };
    taskId = data.taskId;
    title = data.title;
  } catch {
    // If we can't read/parse the file, clean it up and exit
    try {
      unlinkSync(labelFile);
    } catch {
      // Ignore cleanup errors
    }
    process.exit(0);
  }

  // Delete the file so it's only consumed once
  try {
    unlinkSync(labelFile);
  } catch {
    // Ignore cleanup errors
  }

  const sessionTitle = `${taskId} — ${title}`;

  const output: HookOutput = {
    hookSpecificOutput: {
      hookEventName: "UserPromptSubmit",
      sessionTitle,
    } as HookOutput["hookSpecificOutput"] & { sessionTitle: string },
  };

  writeOutput(output);
  process.exit(0);
}

// Entrypoint guard: only run main() when this file is invoked as a script.
// Dynamic imports (the dispatcher's `import("./auto-session-title")`) must
// NOT trigger this — see the header comment (mt#2835).
if (import.meta.main) {
  await main();
}
