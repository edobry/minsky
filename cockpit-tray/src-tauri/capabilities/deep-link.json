{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "deep-link-default",
  "description": "Grants the deep-link plugin access to the cockpit window (mt#2528). Required for tauri-plugin-deep-link to deliver minsky:// URLs to the on_open_url callback. Scoped to the cockpit window label; the SPA is NOT granted any IPC — navigation is Rust→webview eval per ADR-023.",
  "windows": ["cockpit"],
  "permissions": ["deep-link:default"]
}
