node_modules/
dist/
src-tauri/target/
src-tauri/gen/
# bun.lock intentionally untracked: reviewer-bot flags its (valid) JSONC
# trailing commas as invalid JSON (mt#2204). The Rust/Tauri build is pinned
# by src-tauri/Cargo.lock; the JS side is just the Tauri CLI.
bun.lock
