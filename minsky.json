{
  "workflows": {
    "lint": {
      "jsonCommand": "bunx eslint . --format json",
      "fixCommand": "bunx eslint . --fix"
    },
    "test": {
      "jsonCommand": "bun test --reporter json"
    },
    "format": {
      "jsonCommand": "prettier --check . --list-different"
    }
  }
}
